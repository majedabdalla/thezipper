# Telegram
python-telegram-bot==21.6

# MongoDB async driver
motor==3.5.1

# Encrypted zip support (AES-256)
pyzipper==0.3.6

# Environment variable loading
python-dotenv==1.0.1

# HTTP client for health-check pings
httpx==0.27.2

# Async utilities (already in stdlib for 3.11 but pinned for clarity)
# asyncio is built-in; no extra pin needed
